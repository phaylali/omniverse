name: omniverse
description: A new Flutter package.
version: 0.0.1
author: Phaylali
homepage: https://omniverse.dev

environment:
  sdk: ">=2.12.0-0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  # General Packages
  get:
  
  flutter_icons:

  # Firebase Packages
  firebase_auth: 1.0.0
  cloud_firestore: 1.0.0
  firebase_core: 1.0.0
  
  url_launcher:
  


dev_dependencies:
  flutter_test:
    sdk: flutter
  
  build_runner:

flutter:

  uses-material-design: true
  assets:
    - assets/images/