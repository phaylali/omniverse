name: accessories
description: A new Flutter application.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # General Packages
  get:
  get_storage:
  flutter_icons:
  omniverse:
    git:
      url: git://github.com/phaylali/omniverse.git

  # Firebase Packages
  firebase_auth:
  cloud_firestore:
  firebase_core: 
  firebase_analytics:

  # App Specific Packages
  simple_gravatar:
  flutter_sheet_localization:
  
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_sheet_localization_generator:
  build_runner:

flutter:

  uses-material-design: true

