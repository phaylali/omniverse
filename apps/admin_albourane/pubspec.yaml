name: admin_albourane
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  #flutter_localizations:
  #  sdk: flutter

  # General Packages
  get:
  get_storage:
  flutter_icons:
  omniverse:
    path: /home/phaylali/Documents/omniverse

  # Firebase Packages
  firebase_auth: ^0.18.4+1
  cloud_firestore: ^0.14.4
  firebase_core: ^0.5.3
  

  # App Specific Packages
  simple_gravatar:
  #flutter_sheet_localization:
  



dev_dependencies:
  flutter_test:
    sdk: flutter
  #flutter_sheet_localization_generator:
  build_runner:

flutter:

  uses-material-design: true
  assets:
    - assets/images/
